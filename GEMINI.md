# Agent Rules

## 权限设置

用户已授权代理人（Antigravity AI）对本项目拥有完全权限，具体包括：

- **文件操作**：无需确认即可创建、修改、删除任何文件
- **命令执行**：无需确认即可执行任何 shell 命令（包括 git 操作、npm/pip 安装、运行脚本等）
- **代码重构**：可自主进行大范围代码修改，无需逐一审批
- **依赖管理**：可自主安装、更新、删除依赖包

## 工作原则

- 直接执行任务，不要反复询问确认
- 遇到需要决策的情况，优先采用最合理的方案并直接执行
- 执行完毕后简洁地告知用户做了什么，而不是在执行前询问是否可以做
- 如果任务存在重大风险或不可逆操作（如删除重要数据），再行提示用户

## 核心设计准则：全自动向下兼容与静默自愈迁移

- **强向下兼容**：任何新功能、数据结构或目录结构的重构，必须确保旧版本用户 `git pull` 后直接运行 `python main.py` 即可正常工作，无需用户执行任何手动迁移操作。
- **启动时静默自愈与迁移**：
  - 检测到旧版文件目录（如 `state/`、`messages/`、旧版 `.txt`/`.json` 状态文件）时，系统必须在启动阶段自动、无损地将其数据提取并迁移至最新的 SQLite 数据库或新规范目录，并自动清理旧废弃文件。
  - 检测到旧版配置（`config.json` / `.env`）字段时，系统必须在内存中自动规范化兼容，不得破坏用户现有的配置文件。
  - 检测到历史数据存在算法或逻辑偏差（如时区跨月偏差等），系统启动时必须支持全自动扫描和无损自愈纠正。
